<?xml version="1.0" encoding="UTF-8"?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
    xmlns:magic="clr-namespace:MagicGradients;assembly=MagicGradients"
    prism:ViewModelLocator.AutowireViewModel="True"
    BackgroundColor="{DynamicResource background}"
    xmlns:d="http://xamarin.com/schemas/2014/forms/design"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:BackgroundColor="Blue"
    xmlns:yummy="clr-namespace:Xamarin.Forms.PancakeView;assembly=Xamarin.Forms.PancakeView"
    xmlns:controls="clr-namespace:XamarinWeatherApp.Controls"
    x:Class="XamarinWeatherApp.Views.HomePage">
    <ContentPage.Content>
        <Grid
            ColumnSpacing="0"
            RowSpacing="0"
            VerticalOptions="FillAndExpand"
            HorizontalOptions="FillAndExpand">
            <yummy:PancakeView
                VerticalOptions="FillAndExpand"
                HorizontalOptions="FillAndExpand"
                BackgroundGradientStartColor="{Binding OffSetA, Converter={StaticResource ColourOffSetAConverter}}"
                BackgroundGradientEndColor="{Binding OffSetB, Converter={StaticResource ColourOffSetBConverter}}">
            </yummy:PancakeView>
            <StackLayout
                VerticalOptions="FillAndExpand"
                HorizontalOptions="FillAndExpand">
                <StackLayout.Margin>
                    <OnPlatform
                        x:TypeArguments="Thickness">
                        <On
                            Platform="iOS"
                            Value="0,30,0,0" />
                        <On
                            Platform="iOS"
                            Value="0" />
                    </OnPlatform>
                </StackLayout.Margin>
                <controls:NavigationBarControl
                    VerticalOptions="Start"
                    HorizontalOptions="FillAndExpand" />
                <Label
                    Text="{Binding CurrentTime}"
                    VerticalOptions="Start"
                    FontSize="15"
                    FontFamily="{StaticResource LightFont}"
                    TextColor="White"
                    HorizontalOptions="CenterAndExpand">
                </Label>
                <Grid
                    RowSpacing="0"
                    ColumnSpacing="0"
                    VerticalOptions="FillAndExpand"
                    HorizontalOptions="FillAndExpand">
                    <Grid.RowDefinitions>
                        <RowDefinition
                            Height="*" />
                        <RowDefinition
                            Height="*" />
                    </Grid.RowDefinitions>
                    <StackLayout
                        Grid.Row="0"
                        Orientation="Horizontal"
                        VerticalOptions="FillAndExpand"
                        HorizontalOptions="CenterAndExpand">
                        <Label
                            Text="{Binding CurrentTemp}"
                            d:Text="15"
                            FontFamily="{StaticResource LightFont}"
                            TextColor="White"
                            FontSize="150"
                            VerticalOptions="CenterAndExpand"
                            HorizontalOptions="Center" />
                        <Label
                            Text="°"
                            Margin="0,-50,0,0"
                            FontFamily="{StaticResource LightFont}"
                            TextColor="White"
                            FontSize="80"
                            VerticalOptions="Center"
                            HorizontalOptions="Start" />
                    </StackLayout>
                    <StackLayout
                        Grid.Row="1"
                        BackgroundColor="Transparent"
                        VerticalOptions="FillAndExpand"
                        HorizontalOptions="FillAndExpand">
                    </StackLayout>
                </Grid>
            </StackLayout>
        </Grid>
    </ContentPage.Content>
</ContentPage>
